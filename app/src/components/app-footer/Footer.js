import React from 'react';
import { formatFooter } from '../../utils';

export const Footer = () => (
  <footer className="stock-ticker__footer container">
    Anna Chechelnitskaya &copy; {formatFooter}
  </footer>
);